defaults:
  - _self_
  - prob: tsp
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

seed: 1413
split: test
from_pid: 1100
to_pid: 1101
n_processes: 1
order_type: ${prob.order_type}

scorer: gtf
rest_width: 20
# Order in which to sort the nodes
reverse: true
# TSP Heuristic scorer params

hydra:
  output_subdir: null
  run:
    dir: .
